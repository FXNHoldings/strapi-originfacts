# Non-canonical internal links

Base: `http://127.0.0.1:3107` · crawled 60 pages · 5119 distinct link targets · generated by `scripts/check-canonical-links.mjs`

**No non-canonical internal links found.** Every crawled `<a href>` points at its target's canonical URL.

## Pages inheriting the layout's homepage canonical (95)

These targets declare `canonical: /` because the ROOT LAYOUT sets `canonical: '/'` and the page template never overrides it — every such page tells crawlers it is a duplicate of the homepage. The links pointing at them are fine; the fix is a self-referencing canonical (or none) on each affected TEMPLATE. Grouped by path prefix:

- `/flight-routes` — 85 page(s), e.g. `/flight-routes/lax-to-nrt`
- `/legal` — 8 page(s), e.g. `/legal/privacy`
- `/airports` — 1 page(s), e.g. `/airports/hubs`
- `/sitemap` — 1 page(s), e.g. `/sitemap`

