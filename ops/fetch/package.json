{
  "name": "originfacts-fetch",
  "private": true,
  "type": "module",
  "description": "Stage 1 of the airline data pipeline \u2014 archival fetcher. Deliberately separate from the site's package.json so Playwright never installs on a production deploy.",
  "scripts": {
    "fetch": "tsx run.ts",
    "reach": "tsx sweep.ts",
    "ingest-manual": "tsx ingest-manual.ts",
    "extract": "tsx extract.ts",
    "extract:batch": "tsx extract-batch.ts",
    "selectors:discover": "tsx discover-selectors.ts",
    "preflight": "tsx preflight.ts",
    "draft": "tsx draft.ts",
    "draft:batch": "tsx draft-batch.ts",
    "interpret": "tsx interpret.ts",
    "interpret:batch": "tsx interpret-batch.ts",
    "review:build": "tsx build-review.ts",
    "promote": "tsx promote.ts"
  },
  "dependencies": {
    "playwright": "^1.49.0",
    "robots-parser": "^3.0.1",
    "tldts": "^7.4.10"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
